#include "constants.h"
#include <cmath>

const int WIDTH = 800, HEIGHT = 800, FPS = 60,
	SPHERE_RINGS = 32, SPHERE_SLICES = 32;

const float SCALE = 250000000.0f, CAMERA_SPEED = 0.5f,
	CAMERA_ACCELERATION = 1.5f, CAMERA_SLOWDOWN = 0.4f, AU = 1.496f * (float)pow(10, 11);

const double G = 6.67f * (float)pow(10, -11);